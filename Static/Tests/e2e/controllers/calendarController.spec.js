describe('Calendar Integration', function(){
    it('Should return importan dates (200 status)', inject(function($http) {
        
        // var $scope = {};
      
        // /* Code Under Test */
        // $http.get('http://localhost/foo')
        //   .success(function(data, status, headers, config) {
        //     $scope.valid = true;
        //     $scope.response = data;
        //   })
        //   .error(function(data, status, headers, config) {
        //     $scope.valid = false;
        // });
        // /* End */
      

      
        // expect($scope.valid).toBe(true);
        // expect($scope.response).toEqual({ foo: 'bar' });
      
      }));
});